import classes from "./MainCards.module.scss";

const MainCards = () => {
  return (
    <div className={classes.container}>
      <div className={classes.mainCard}>test</div>
      <div className={classes.trailerCard}>test</div>
    </div>
  );
};

export default MainCards;
